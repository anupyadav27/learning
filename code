kubectl apply -k "github.com/kubernetes-sigs/aws-ebs-csi-driver/deploy/kubernetes/overlays/stable/ecr/?ref=master"
kubectl get pods -n kube-system | grep ebs-csi
{
  "Version": "2012-10-17",
  "Statement": [
    {
      "Effect": "Allow",
      "Action": [
        "ec2:AttachVolume",
        "ec2:DetachVolume",
        "ec2:CreateVolume",
        "ec2:DeleteVolume",
        "ec2:ModifyVolume"
      ],
      "Resource": "*"
    }
  ]
}


kubectl get pvc -n data-collection
kubectl get pv
kubectl delete pvc nifi-pvc -n data-collection
kubectl delete pv nifi-pv
kubectl get pods -n data-collection
kubectl describe pod nifi-0 -n data-collection
